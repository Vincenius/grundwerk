---
import { products } from "../data/products";
import Product from "./Product.astro";

// Add showall prop, default to true
const { showAll = false } = Astro.props;

const allProducts = !showAll ? products.slice(0, 4) : products;

// Split products into even and odd index arrays
let evenProducts = allProducts.filter((_, i) => i % 2 === 0);
let oddProducts = allProducts.filter((_, i) => i % 2 !== 0);
---

<section class="relative my-40 px-8" id="products-section">
  <div class="flex gap-8 max-w-6xl mx-auto md:flex-row flex-col">
    <div class="flex flex-col gap-8">
      {
        !showAll && (
          <div>
            <h2
              id="products-heading"
              class="font-bold text-4xl font-thin opacity-0 mb-8 underline"
            >
              Our Products
            </h2>
            <p
              id="products-description"
              class="text-lg text-gray-600 max-w-xl opacity-0"
            >
              Designer essentials that combine style, innovation, and effortless
              performance for your everyday life.
            </p>
          </div>
        )
      }

      {evenProducts.map((product) => <Product product={product} />)}
    </div>
    <div class="flex flex-col gap-8">
      {oddProducts.map((product) => <Product product={product} />)}

      {
        !showAll && (
          <div class="flex justify-center">
            <a
              href="/products"
              class="inline-block mt-8 px-8 py-3 bg-black text-white font-semibold rounded-full shadow-lg hover:bg-gray-800 transition-colors duration-300 text-lg"
            >
              Show all products
            </a>
          </div>
        )
      }
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  window.addEventListener("DOMContentLoaded", () => {
    const section = document.getElementById("products-section");
    const heading = document.getElementById("products-heading");
    const description = document.getElementById("products-description");
    const cards = section.querySelectorAll("[data-product-card]");
    const showAllBtn = section.querySelector("a.inline-block");

    gsap.set([heading, description, ...cards, showAllBtn], {
      opacity: 0,
      y: 100,
    });

    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: section,
        start: "top 80%",
        once: true,
      },
    });

    tl.to(heading, {
      opacity: 1,
      y: 0,
      duration: 0.4,
      ease: "power2.out",
    })
      .to(
        description,
        {
          opacity: 1,
          y: 0,
          duration: 0.4,
          ease: "power2.out",
        },
        "-=0.2",
      )
      .to(
        cards,
        {
          opacity: 1,
          y: 0,
          duration: 0.2,
          stagger: 0.08,
          ease: "power2.out",
        },
        "-=0.2",
      )
      .to(
        showAllBtn,
        {
          opacity: 1,
          y: 0,
          duration: 0.3,
          ease: "power2.out",
        },
        "-=0.1",
      );
  });
</script>
